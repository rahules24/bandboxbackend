name: Keep Fly Alive

on:
  schedule:
    - cron: "*/30 2-17 * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl Fly App
        run: curl -s https://bandboxbackend.fly.dev
